# Runtime state written by the DE1app on the tablet -- personal data,
# never committed (per the plugin's packaging rules).
*.tdb

# Espresso databases and shot history -- personal data, never public.
*.db
*.sqlite
*.sqlite3
history/
history_v2/
history_archive/
*.shot

# Local backup/trash/log folders (never shipped)
backups/
trash/
logs/
*.bak
trash_manifest.txt
delete_log.txt
edit_manifest.txt
edit_log.txt

# Version archives
*Archive/

# Internal working notes -- kept local, never published.
PROJECT_STATE.md

# Local dev tooling -- contains absolute paths from this machine.
tools/

# OS junk
desktop.ini
Thumbs.db
.DS_Store
